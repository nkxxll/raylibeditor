need to go through the table that is argument to the update state function and parse it
need to create helper functions in either lua or c that can create slide (lua might be better here)
need to add rendering logic for state with a styling system that I might build myself
